# Format a p-value according to current clerk_options.

Format a p-value according to current clerk_options.

## Usage

``` r
.fmt_p(
  x,
  p_digits = NULL,
  p_threshold = NULL,
  p_style = NULL,
  stars = NULL,
  star_thresholds = NULL
)
```
