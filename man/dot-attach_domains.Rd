% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render.R
\name{.attach_domains}
\alias{.attach_domains}
\title{Attach domain (and, for nested domains, subdomain) columns to a table.}
\usage{
.attach_domains(tbl, domains, domain_other = "")
}
\description{
\code{domains} accepts two shapes, and the two can be mixed within one list:
\itemize{
\item flat:   list("Metabolic" = c("hdl", "glucose"))
\item nested: list("Mental health" = list("Baseline"   = c("bdi_bl", ...),
"Follow-up 1" = c("bdi_fu1", ...)))
A flat entry gets subdomain = "". A nested entry's subdomain is the name
of its inner list element. \code{domain_group} is the compound label used as
gt's single row-group column -- the domain and subdomain names joined
with an em dash when there's a subdomain, or just the domain name when
there isn't. reactable ignores \code{domain_group} and groups on
domain/subdomain directly for true nesting.
Both \code{domain} and \code{domain_group} are returned as factors with levels in
the order variables were supplied in \code{domains}, so row-group order in the
rendered table always matches the order the user wrote them in, rather
than falling back to alphabetical.
}
}
\keyword{internal}
