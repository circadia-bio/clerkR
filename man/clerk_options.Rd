% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/options.R
\name{clerk_options}
\alias{clerk_options}
\title{clerkR session options}
\usage{
clerk_options(
  digits = NULL,
  r_digits = NULL,
  p_digits = NULL,
  p_threshold = NULL,
  p_style = NULL,
  stars = NULL,
  star_thresholds = NULL,
  fdr_ns = NULL,
  fdr_alpha = NULL,
  fdr_ns_label = NULL,
  reset = FALSE
)
}
\arguments{
\item{digits}{Integer. Decimal places for continuous summary statistics
(default \code{2}).}

\item{r_digits}{Integer. Decimal places for correlation coefficients and h²
estimates (default \code{3}).}

\item{p_digits}{Integer. Decimal places for p-values (default \code{3}).}

\item{p_threshold}{Numeric. Raw p-values \strong{below} this are shown as
\code{"< {threshold}"} rather than as a decimal (default \code{0.001}). Display
only — not a significance threshold.}

\item{p_style}{Character string controlling p-value display style:
\describe{
\item{\code{"apa"}}{APA format: \verb{= 0.032}, \verb{< 0.001} (default).}
\item{\code{"plain"}}{Plain decimal: \code{0.032}, \verb{< 0.001}.}
\item{\code{"stars"}}{Significance stars only, no numeric p.}
\item{\code{"stars_p"}}{Stars alongside numeric p.}
}}

\item{stars}{Logical. Append significance stars (\code{*}/\verb{**}/\verb{***}) to p-value
cells (default \code{FALSE}).}

\item{star_thresholds}{Numeric vector of length 3. P-value cutoffs for one,
two, and three stars respectively (default \code{c(0.05, 0.01, 0.001)}).}

\item{fdr_ns}{Logical. Replace the FDR-corrected p-value cell with
\code{fdr_ns_label} when \code{p(FDR) >= fdr_alpha} (default \code{TRUE}). The raw
p-value column is unaffected.}

\item{fdr_alpha}{Numeric. Alpha level applied to the \strong{BH-adjusted}
p-value. Cells where \code{p(FDR) >= fdr_alpha} are labelled \code{fdr_ns_label}
(default \code{0.05}). Compared against the BH-adjusted p, not the raw p.}

\item{fdr_ns_label}{Character string for non-surviving FDR cells
(default \code{"ns"}).}

\item{reset}{Logical. Restore factory defaults (default \code{FALSE}).}
}
\value{
A named list of current option values, returned invisibly.
}
\description{
\code{clerk_options()} gets or sets session-level formatting defaults used by all
\verb{tbl_*} constructors. Defaults are loaded automatically when the package is
attached and follow biomed/APA conventions.

Call with no arguments to inspect current settings. Call with named
arguments to change one or more. Call with \code{reset = TRUE} to restore factory
defaults.
}
\examples{
clerk_options()
clerk_options(p_style = "apa", stars = TRUE)
clerk_options(fdr_alpha = 0.01)
clerk_options(reset = TRUE)

}
